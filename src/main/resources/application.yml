spring:
  datasource:
    url: ${CLEARDB_DATABASE_URL:jdbc:mysql://localhost:3306/test?serverTimezone=UTC}
    username: root
    password:

  jpa:
    show-sql: true

    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect


  data:
    mongodb:
      uri: ${MONGODB_IGP_URI:mongodb://localhost:27017/seasson_10?retryWrites=true&w=majority}

com:
  ckno:
    igp-front-url: ${IGP_FRONT_URL:http://localhost:3000}